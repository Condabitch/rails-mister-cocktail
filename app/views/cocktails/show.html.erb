<div class='container'>
  <div class='row'>
    <div class='col-md-6 mx-auto'>
      <h1>Hmmmm, let's have a <%= @cocktail.name %></h1>
        <h3>How to prepare your cocktail</h3>
            <% @doses.each do |dose| %>
            <p><%= dose.description %> of ingredient<%= dose.ingredient_id %></p>
          <% end %>
          <%= link_to 'Add a Dose', new_dose_path %> <%= link_to 'back', cocktails_path, class: "btn btn-primary" %><br>
    </div>
  </div>
</div>

